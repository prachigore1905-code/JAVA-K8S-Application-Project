apiVersion: v1
kind: Service

metadata:
  name: college-svc

spec:
  type: ClusterIP

  selector:
    app: college-app

  ports:
    - name: http
      protocol: TCP
      port: 8090
      targetPort: 8090
