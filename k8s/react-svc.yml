apiVersion: v1
kind: Service

metadata:
  name: college-frontend-service

spec:
  type: LoadBalancer

  selector:
    app: college-frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
